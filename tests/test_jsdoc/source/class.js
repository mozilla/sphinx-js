/**
 * Class doc.
 *
 * @example Example in class
 */
class Foo {
    /**
     * Constructor doc.
     *
     * @arg ho A thing
     * @example Example in constructor
     */
    constructor(ho) {}

    /**
     * Setting this also frobs the frobnicator.
     */
    get bar() {
      return this._bar;
    }
    set bar(baz) {
      this._bar = _bar;
    }

    /**
     * Private method.
     *
     * @private
     */
    secret() {}
}
